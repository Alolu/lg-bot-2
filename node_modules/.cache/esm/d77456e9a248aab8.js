"main";_a24‍.x([["default",()=>Module]]);var command;
               class Module {
    constructor(){
        this.Command = command;
        config = {
            name: 'DEFAULT_NAME',
            description: 'DEFAULT_DESC'
        }
    }

    static setCommand(Command,bot){
        command = Command;

        var utilVar = {
            [this.name]: { lol : 3}
        }
        var newBot = Object.assign(bot,utilVar);
        _a24‍.g.console.log(newBot);

        return newBot;
    }

    setBot(bot) {
        this.bot = bot;
        _a24‍.g.console.log(this.constructor.name);
    }
}