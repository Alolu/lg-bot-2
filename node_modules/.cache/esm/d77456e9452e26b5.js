"main";_452‍.x([["default",()=>Module]]);let fs;_452‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let path;_452‍.w("path",[["default",["path"],function(v){path=v}]]);let Discord;_452‍.w("discord.js",[["default",["Discord"],function(v){Discord=v}]]);



var command,bot
               class Module {
    constructor(){
        this.Command = command
        this.bot = bot
        this.config = {
            name: 'DEFAULT_NAME',
            description: 'DEFAULT_DESC'
        }
    }

    getCommands(){
        return new Discord.Collection(
            this.mkObjectFromArrays(Object.getOwnPropertyNames,Object.values)
        );;
    }

    mkObjectFromArrays(key,values){
        var result
        keys.forEach((key, i) => {
            if(values[i] instanceof command){
                values[i].setModule(this.name)
                result[key] = values[i]
            }
        });
        return result
    }

    static setCommand(Command){
        command = Command

        var utilVar = {
            [this.name]: { lol : 3}
        }
        var newBot = Object.assign(bot,utilVar)
        _452‍.g.console.log(newBot)

        return newBot
    }

    static getUtil(utilFolder){
        if(fs.existsSync(utilFolder)){
            fs.readdirSync(utilFolder).forEach((utilFile)=>{
                //import Util from path.join(utilFolder,utilFile)
                var Util = require(path.join(utilFolder,utilFile))
                new Util()
            })
        }
    }

    static setBot(Bot) {
        bot = Bot
    }
}