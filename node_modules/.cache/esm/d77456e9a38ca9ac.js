"main";_a38‍.x([["default",()=>Module]]);let fs;_a38‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let path;_a38‍.w("path",[["default",["path"],function(v){path=v}]]);let Discord;_a38‍.w("discord.js",[["default",["Discord"],function(v){Discord=v}]]);



var command,bot
               class Module {
    constructor(){
        this.Command = command
        this.bot = bot
        this.config = {
            name: 'DEFAULT_NAME',
            description: 'DEFAULT_DESC'
        }
    }

    getCommands(){
        var Commands = this.mkObjectFromArrays(Object.getOwnPropertyNames(this),Object.values(this))
        return Commands
    }

    mkObjectFromArrays(keys,values){
        var result = {}
        keys.forEach((key, i) => {
            if(values[i] instanceof command){
                values[i].setModule(this.constructor.name)
                result[key] = values[i]
            }
        });
        return result
    }

    static setCommand(Command,bot){
        command = Command

        var utilVar = {
            [this.name]: { lol : 3}
        }
        var newBot = Object.assign(bot,utilVar)

        return newBot
    }

    static getUtil(utilFolder){
        if(fs.existsSync(utilFolder)){
            fs.readdirSync(utilFolder).forEach((utilFile)=>{
                //import Util from path.join(utilFolder,utilFile)
                var Util = require(path.join(utilFolder,utilFile))
                new Util()
            })
        }
    }

    static setBot(Bot) {
        bot = Bot
    }
}